{
  "name": "Negociar",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "turbo run prisma:generate --filter=@Negociar/db",
    "db:migrate": "turbo run prisma:migrate --filter=@Negociar/db"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "turbo": "^2.5.8",
    "typescript": "~6.0.2"
  }
}
